FROM apache/superset:latest

# Install PostgreSQL driver into the Superset virtual environment
# The Superset image uses /app/.venv as the virtual environment
RUN /app/.venv/bin/pip install --no-cache-dir psycopg2-binary
