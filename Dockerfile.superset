FROM apache/superset:latest

# Install PostgreSQL driver
# The apache/superset:latest image installs to the global Python site-packages
# which is accessible when Superset runs
USER root
RUN pip install --no-cache-dir --target /app/.venv/lib/python3.10/site-packages psycopg2-binary
USER superset
